% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PACS_test_cumulative.R
\name{pacs_test_cumu}
\alias{pacs_test_cumu}
\title{likelihood ratio test with PACS -- cumulative logit}
\usage{
pacs_test_cumu(
  covariate_meta.data,
  formula_full,
  formula_null,
  pic_matrix,
  max_T = 2,
  cap_rates,
  par_initial_null = NULL,
  par_initial_full = NULL,
  n_cores = 1,
  method = c("stacked", "exact"),
  capture = c("B", "A")
)
}
\arguments{
\item{covariate_meta.data}{A data.frame with columns representing the
covariates and rows representing cells}

\item{formula_full}{A formula object representing the full model. For
example, ~ cell_type + batch}

\item{formula_null}{A formula object representing the null model. For
example, ~ batch}

\item{pic_matrix}{The input region-by-cell PIC matrix}

\item{max_T}{The maximum accessibility category considered, default = 2.
For \code{method = "exact"}, observed counts greater than \code{max_T} are
top-coded. Under \code{capture = "B"} this is exact and the final category
means \code{max_T} or more. Under \code{capture = "A"} it is an approximation:
capping and fragment thinning do not commute, so \code{max_T} acts as a cap on
the \emph{latent} count and should be set high enough that observed counts
above it are rare. A warning reports how many were top-coded.}

\item{cap_rates}{A vector of capturing probability for each cell}

\item{par_initial_null}{Initialized values of estimated parameters for the
null model, we do not
need to specify unless there are reasons to do so. Default = NULL}

\item{par_initial_full}{Initialized values of estimated parameters for the
null model, we do not
need to specify unless there are reasons to do so. Default = NULL}

\item{n_cores}{number of cores for multi-core computation}

\item{method}{One of \code{"stacked"} (default, back-compat) or \code{"exact"}.
\code{"stacked"} uses the original stack-and-treat-as-binary approximation.
\code{"exact"} uses the proper cumulative-logit likelihood with capture-rate
correction (\code{notes/cumulative_logit_math.md}) and an unpenalized
maximum-likelihood fit. Exact-path p-values are ordinary
likelihood-ratio tests and are \code{NA} for non-converged or boundary fits.}

\item{capture}{Capture model for \code{method = "exact"}; ignored by
\code{method = "stacked"}. \code{"B"} (default, back-compat) is cell-level
all-or-nothing dropout: with probability \code{q_i} the cell is observed
perfectly, otherwise its count collapses to zero. \code{"A"} is fragment-level
thinning, \code{M_i | Y_i = k ~ Binomial(k, q_i)}, which is the natural
generalisation of the binary PACS capture model and the appropriate
choice when a partially captured cell should still be able to yield a
nonzero count. The two coincide at \code{max_T = 1} and diverge as counts of
2 or more become common. See Options A and B in
\code{notes/cumulative_logit_math.md}.}
}
\value{
A list of two elements. \code{pacs_converged} has length
\code{2 * n_peaks}, with null-fit statuses followed by full-fit statuses. For
the exact path, status 1 means converged, 2 means a singular or non-finite
scoring system, 3 means the iteration limit was reached, 4 means
step-halving found no acceptable update, and 5 means the supplied starting
value had a non-finite log-likelihood. \code{pacs_p_val} contains one p-value per
peak; exact-path inference is \code{NA} unless both statuses are 1 and both fits
are interior.
}
\description{
likelihood ratio test with PACS -- cumulative logit
}
\details{
The unpenalized exact MLE can fail to exist or have singular
information for very sparse peaks. In review simulations, the exact-path
\code{NA} rate rose from 0\% for dense peaks to 3\% for moderately sparse peaks
(\code{n = 300}, \code{alpha = c(-2.5, -4)}), 38\% for still sparser peaks
(\code{n = 300}, \code{alpha = c(-3.5, -5)}), and 46\% with fewer cells
(\code{n = 100}, \code{alpha = c(-2.5, -4)}). These rates are scenario-specific,
not general guarantees. Withholding a p-value is a conservative failure
policy for the affected peak—it avoids turning a failed fit into a false
positive—but the resulting loss of analyzable peaks reduces power.
}
